# PremiaFlow — 프로젝트 지침

생명보험 보험료 단계별 산출 웹앱. 상세 설계는 `PremiaFlow_설계서_v1.2.md`(이하 "설계서")가 단일 기준이다. 이 문서와 설계서가 충돌하면 설계서를 따르고, 이 문서를 갱신한다.

## 1. 개요와 목표

- 수지상등 방식 보험료 산출을 위저드형 파이프라인(모듈 M01~M11)으로 구현. 강의·실습용.
- **정확도 최우선**: 편의성·속도·미관보다 계산 정확도가 항상 우선한다.
- 현재 페이즈: **P5 로컬 파트 완료**(localAdapter 자동 저장·복원, M11 통합 계산표·CSV/XLSX 내보내기, 라이브러리 페이지, 키보드 단계 이동) → **P5 클라우드 파트 대기**: Supabase 프로젝트 연동(매직링크·RLS·게스트 가져오기)·Vercel 배포는 사용자 계정 연결 필요(설계서 §6.5). 페이즈별 체크포인트 방식(설계서 §7.1).
- 이연 항목: XLSX 업로드·M11 통합 계산표·내보내기(P5, SheetJS), 수식 드래그 블록 모드·KaTeX 표기(v1.x — 텍스트 모드+클릭 팔레트로 대체), 공용탭 모듈의 참조 탭 수 배지(§3.2 (f)), `lib/engine/graph.ts` 명시적 DAG(전체 순차 재계산으로 충분).
- M10 수식은 자산 **코드**로 참조한다(텍스트 모드 특성) — 코드 변경 시 수식에 미정의 참조 오류가 표시되어 사용자가 갱신한다.

## 2. 기술 스택·코딩 규칙

- Next.js 15 App Router · TypeScript strict · Tailwind v4 · shadcn/ui(TweakCN 토큰) · Supabase(P5부터).
- 모든 계산은 **클라이언트**에서 수행. 서버 컴포넌트는 라우팅·정적 콘텐츠 위주.
- `async/await` 사용, callback 패턴 지양. UI 문자열은 한국어.
- 경로 별칭 `@/*`. 폰트: 본문 Pretendard, 숫자·코드 JetBrains Mono(tabular-nums).

## 3. 계산 정확도 원칙

- 내부 계산은 float64로 **중간 반올림 없이** 진행. 표시 자릿수는 별도 관리. 최종 보험료에만 단수처리 옵션 적용.
- 모든 계산·코드 생성은 **결정론적**: 동일 입력 → 동일 출력.
- 엔진·모듈 계산을 수정하면 골든 테스트(G1~G5, `docs/domain/golden-cases.md`)를 **반드시 실행**하고, 고정 기대값과 완전 일치해야 통과다. 기대값은 엔진과 독립된 Python 이중 산출로 확정한다(설계서 §7.3).

## 4. 모듈·자산 명명 규칙

- 자산은 표시명(한글 허용)과 코드 식별자(`^[a-z][a-z0-9_]{0,29}$`, `types/assets.ts`의 `ASSET_CODE_RE`)를 분리한다.
- 코드 식별자 유일성: 일반 탭 자산은 탭 범위, 공용탭 자산은 워크북 전체 범위. 내부 참조는 항상 자산 ID로 한다(설계서 §3.3).

## 5. 파이프라인 JSON 스키마

- 단일 출처: `types/pipeline.ts`(+ `types/modules.ts`, `types/assets.ts`). 저장·v2.0 문서 변환의 목표 형식.
- 스키마 변경 시 `SCHEMA_VERSION`을 올리고 마이그레이션을 제공한다.

## 6. 서브에이전트 오케스트레이션

- 서브에이전트 4종(설계서 §6.3): `db-architect`(P5), `calc-core`(P1~), `codegen-engineer`(P4), `ui-builder`(P2~). AGENT.md는 각 에이전트가 처음 필요한 페이즈에서 작성한다.
- 서브에이전트 간 직접 호출 금지, 메인이 조율. 큰 산출물은 `/output/step{n}_*.json`에 저장하고 경로만 전달.

## 7. 검증 루틴 (순서대로)

1. `npm run build` — 빌드·타입 오류 0
2. `npx vitest run` — 단위·골든 테스트 (P1부터)
3. Python 대사 테스트(pytest) — 생성 코드 실행값 = 엔진값 (P4부터)

## 8. 금지사항

- 계산 경로(엔진·수식 평가·코드 생성·파싱)에 **LLM 사용 금지** — 전부 결정론 코드.
- `eval`·`new Function` 금지 — 수식은 자체 토크나이저·파서·평가기로만.
- RLS 우회 금지. `service_role` 키는 서버 전용, 클라이언트 노출 금지.
- `.env` git 커밋 금지.
